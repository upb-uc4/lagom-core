apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: postgres
spec:
  type: NodePort
  ports:
    - name: "psql"
      protocol: "TCP"
      port: 5432
  selector:
    app: postgres